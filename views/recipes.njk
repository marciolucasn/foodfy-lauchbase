{% extends "layout.njk" %}

{% block head %}
<title>Receitas - Foodfy</title>
{% endblock head %}

{% block content %}
    <main>

        <section class="cards">

            {% for item in recipes %}
            <div class="card" id="{{item.title}}">
                <img src="{{item.image}}" alt="Imagem de {{item.title}}">
                <h4>{{item.title}}</h4>
                <p>por {{item.author}}</p>
            </div>
            {% endfor %}
            {% for item in recipes %}
            <div class="card" id="{{item.title}}">
                <img src="{{item.image}}" alt="Imagem de {{item.title}}">
                <h4>{{item.title}}</h4>
                <p>por {{item.author}}</p>
            </div>
            {% endfor %}

        </section>

    </main>
{% endblock content %}